import React from 'react'
import styled from 'styled-components'

function Chat() {
    return (
        <ChatContainer>
            
        </ChatContainer>
    )
}

export default Chat

const ChatContainer = styled.div`` 
